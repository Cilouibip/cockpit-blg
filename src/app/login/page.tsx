'use client';
import { useState } from 'react';
export default function Login(){const [error,setError]=useState('');const [busy,setBusy]=useState(false);return <main className="login-page"><section className="login-card"><p className="blg-eyebrow">BLG STUDIO · ACCÈS PRIVÉ</p><h1>Ton activité,<br/>au même endroit.</h1><p>Entre le mot de passe du cockpit.</p><form onSubmit={async e=>{e.preventDefault();setBusy(true);setError('');const data=new FormData(e.currentTarget);try{const r=await fetch('/api/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:data.get('password')})});const result=await r.json();if(!r.ok)throw new Error(result.error||'Connexion impossible.');location.assign('/');}catch(e){setError(e instanceof Error?e.message:'Connexion impossible.');}finally{setBusy(false);}}}><label className="blg-field">Mot de passe<input name="password" type="password" autoComplete="current-password" required maxLength={256}/></label><button className="blg-button blg-primary" type="submit" disabled={busy}>{busy?'Connexion…':'Ouvrir le cockpit'}</button><p role="alert">{error}</p></form></section></main>;}
